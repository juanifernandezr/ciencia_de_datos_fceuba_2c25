# Definir Working directory
# Yo estoy trabajando en un mi disco D y en la carpeta del curso
# Asi que voy a definir todo el path hasta las carpetas que estoy usando
setwd(r'(D:\Docencia\UBA\Ciencia de datos\ciencia_de_datos_fceuba_2c25\ejemplo_workflow_tp_final)')
# Rutas
# setwd(r'(D:\Docencia\UBA\Ciencia de datos\ciencia_de_datos_fceuba_2c25\ejemplo_workflow_tp_final)')
# Vuelvo a cargar el working directory, por las dudas de que haya cerrado la sesion
auxiliar <- 'auxiliar'
instub <- 'raw'
outstub <- 'data/input'
# Cargar datos
pib <- read_csv(file.path(instub,'pib_argendata.csv'))
# Librerias
library(tidyverse)
# Rutas
# setwd(r'(D:\Docencia\UBA\Ciencia de datos\ciencia_de_datos_fceuba_2c25\ejemplo_workflow_tp_final)')
# Vuelvo a cargar el working directory, por las dudas de que haya cerrado la sesion
auxiliar <- 'auxiliar'
instub <- 'raw'
outstub <- 'data/input'
# Cargar datos
pib <- read_csv(file.path(instub,'pib_argendata.csv'))
# Paises
paises <- isocountry::isocountry
paises <- paises %>%
select(alpha_3,region_name) %>%
distinct()
# Agregar continente
pib <- pib %>%
left_join(paises,by=c('geocodigoFundar'='alpha_3'))
pib
lm(industry_gdp )
lm(industry_gdp ~ region_name,data=pib %>% filter(anio == 2023))
summary(lm(industry_gdp ~ region_name,data=pib %>% filter(anio == 2023)))
options(scipen=999)
summary(lm(industry_gdp ~ region_name,data=pib %>% filter(anio == 2023)))
